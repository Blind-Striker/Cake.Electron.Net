trigger:
  branches:
    include:
      - "*"
  paths:
    include:
      - "*"
    exclude:
      - "**/*.md"
pr:
  branches:
    include:
      - "*"
pool:
  name: Hosted macOS
steps:
  - bash: "sudo curl -o /usr/local/bin/nuget.exe https://dist.nuget.org/win-x86-commandline/latest/nuget.exe && alias nuget="mono /usr/local/bin/nuget.exe" && mkdir ./testrunner && ls"
    displayName: "Init Task"
    
  - task: UseDotNet@2 
    displayName: ".NET Core 3.1.x"
    inputs:
      version: '3.1.x'
      packageType: sdk

  - task: UseDotNet@2 
    displayName: ".NET Core 2.1.x"
    inputs:
      version: '2.1.x'
      packageType: sdk

  - task: Bash@3
    displayName: 'Compile & Tests'
    inputs:
      targetType: filePath
      filePath: ./build.sh

  - task: PublishTestResults@2
    inputs:
      testResultsFormat: "VSTest"
      testResultsFiles: "**/*.trx"
      testRunTitle: "Cake.Electron.Net.Tests"
